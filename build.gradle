wrapper {
	gradleVersion = '4.3'
}

allprojects {
	repositories {
		jcenter()
	}
	
	pluginManager.withPlugin('java') {
		jar {
			archiveName = "${rootProject.name}-${projectDir.name}.jar"
		}
	}
}