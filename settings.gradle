
rootProject.name = 'uulib-reckon'


['reckonStrategy', 'reckonGroovyDsl'].each { String projectName ->
	include projectName
	String dirName = projectName.replaceAll('\\p{Upper}') { "-${it.toLowerCase()}" }
	project(":$projectName").projectDir = new File(settingsDir, dirName)
}